{% extends 'base.html.twig' %}

{% block title %}Usuário{% endblock %}

{% block body %}
    <h1>Dados</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ usuario.nome }}</td>
            </tr>
            <tr>
                <th>E-mail</th>
                <td>{{ usuario.email }}</td>
            </tr>
            <tr>
                <th>Senha</th>
                <td>{{ usuario.senha }}</td>
            </tr>
            <tr>
                <th>Nível de acesso</th>
                <td>{{ usuario.nivelAcesso }}</td>
            </tr>
        </tbody>
    </table>

     <div style="font-weight:bold" class="form-row col-4 ml-4 mt-0 text-">
    <a href="{{ path('usuario_index') }}">Voltar para lista</a>  
    <div class="ml-4">
    <a href="{{ path('usuario_edit', {'id': usuario.id}) }}">Editar</a>
    </div>
    </div>
    </div>
    <div style="font-weight:bold" class="col-4 ml-4 mt-0 text-left">
      {{ include('usuario/_delete_form.html.twig') }}
    </div>
{% endblock %}

