{% extends 'base.html.twig' %}

{% block title %}Clientes{% endblock %}

{% block body %}
    <h1>Dados</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cliente.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ cliente.nome }}</td>
            </tr>
            <tr>
                <th>Data de nascimento</th>
                <td>{{ cliente.dataDeNascimento ? cliente.dataDeNascimento|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Telefone</th>
                <td>{{ cliente.telefone }}</td>
            </tr>
            <tr>
                <th>Endereço</th>
                <td>{{ cliente.endereco }}</td>
            </tr>
            <tr>
                <th>CPF</th>
                <td>{{ cliente.cpf }}</td>
            </tr>
        </tbody>
    </table>
     <div style="font-weight:bold" class="form-row col-4 ml-4 mt-0 text-">
    <a href="{{ path('clientes_index') }}">Voltar para lista</a>  
    <div class="ml-4">
    <a href="{{ path('clientes_edit', {'id': cliente.id}) }}">Editar</a>
    </div>
    </div>
    </div>
    <div style="font-weight:bold" class="col-4 ml-4 mt-0 text-left">
      {{ include('clientes/_delete_form.html.twig') }}
    </div>
{% endblock %}
