{% extends 'base.html.twig' %}

{% block title %}Empresa{% endblock %}
{% block stylesheets %}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.2/css/bootstrap.css">
<link rel="stylesheet" href="https://cdn.datatables.net/1.10.22/css/dataTables.bootstrap4.min.css">
{% endblock %}
{% block body %}
    {# <h1>Empresa</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Razão social</th>
                <th>CNPJ</th>
                <th>Endereco</th>
                <th>Telefone</th>
                <th>E-mail</th>
                <th>Ramo de atividade</th>
                <th>Número de funcionários</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
        {% for empresa in empresas %}
            <tr>
                <td>{{ empresa.id }}</td>
                <td>{{ empresa.razaoSocial }}</td>
                <td>{{ empresa.cnpj }}</td>
                <td>{{ empresa.endereco }}</td>
                <td>{{ empresa.telefone }}</td>
                <td>{{ empresa.email }}</td>
                <td>{{ empresa.ramoDeAtividade }}</td>
                <td>{{ empresa.numeroDeFuncionarios }}</td>
                <td>
                    <a href="{{ path('empresa_show', {'id': empresa.id}) }}">Mostrar</a>
                    <a href="{{ path('empresa_edit', {'id': empresa.id}) }}">Editar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('empresa_new') }}">Criar novo</a> #}



<div class="shadow p-3 mb-5 bg-white rounded">
    <div class="text-right mb-3">
    </div>
    <table width="100%" id="lista1" class="table table-striped table-hover">
        <thead>
        </thead>
        <tbody>
        </tbody>
    </table>
</div>
{% endblock %}
{% block javascript %}
<script src="https://code.jquery.com/jquery-3.5.1.js"></script>
<script src="https://cdn.datatables.net/1.10.22/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/1.10.22/js/dataTables.bootstrap4.min.js"></script>
<script>
var tabela = $("#lista1").DataTable({
  paging: true,
    lengthChange: true,
    searching: true,
    ordering: true,
    info: true,
    autoWidth: true,
    responsive: true,
    columns: 
    [
        { data: "razaoSocial", title: "Razão Social"},
        { data: "cnpj", title: "CNPJ"},
        { data: "endereco", title: "Endereço"},
        { data: "telefone", title: "Telefone"},
        { data: "email", title: "E-Mail"},
        { data: "ramoDeAtividade", title: "Ramo de Atividade"},
        { data: "numeroDeFuncionarios", title: "Número de Funcionários"},
    ],ajax: {
      url: "{{ path('empresa_ajax') }}",
      type: "POST",
      dataSrc: function(resp){
        console.log(resp);
        
        $('#loading').remove();    
        return resp;
      }
    }
});
</script>

{% endblock %}
